{
  "name": "evi",
  "private": true,
  "type": "module",
  "scripts": {
    "test:apps": "node --test --test-concurrency=1 \"bonuses/**/*.test.mjs\" \"cards/**/*.test.mjs\" \"smith/**/*.test.mjs\" \"shell/**/*.test.mjs\"",
    "test:scripts": "node --test --test-concurrency=1 \"scripts/**/*.test.mjs\" \"shell/lib/**/*.test.mjs\"",
    "test": "npm run test:scripts && npm run test:apps",
    "build:image-atlases": "node scripts/build-image-atlases.js",
    "build:stamp-bonuses": "node scripts/stamp-bonuses-cache.js",
    "build": "npm run build:image-atlases && npm run build:stamp-bonuses"
  },
  "dependencies": {
    "sharp": "^0.34.2"
  }
}
